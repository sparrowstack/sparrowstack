// import { type IValidateParams } from '@sparrowstack/tool';

export const validateGetWeatherDataToolCall =
	async (/*validateParams: IValidateParams*/) => {
		const shouldCallTool = true;

		return shouldCallTool;
	};

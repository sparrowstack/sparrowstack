{
	"name": "@sparrowstack/sparrow",
	"version": "0.0.4",
	"description": "Sparrow - AI Agent Framework",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"license": "MIT",
	"publishConfig": {
		"access": "restricted"
	},
	"author": {
		"name": "SparrowStack",
		"url": "https://sparrowstack.ai"
	},
	"scripts": {
		"format": "prettier --write \"src/**/*.{js,ts,json,md}\"",
		"format:check": "prettier --check \"src/**/*.{js,ts,json,md}\"",
		"lint": "eslint \"src/**/*.{js,ts}\"",
		"lint:fix": "eslint \"src/**/*.{js,ts}\" --fix",
		"build:js": "bun build src/index.ts --outdir dist --target bun",
		"build:types": "tsc --declaration --emitDeclarationOnly --outDir dist",
		"build": "bun build:js && bun build:types",
		"link": "bun link",
		"unlink": "bun unlink"
	},
	"dependencies": {
		"@sparrowstack/agent": "workspace:*",
		"@sparrowstack/core": "workspace:*"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}

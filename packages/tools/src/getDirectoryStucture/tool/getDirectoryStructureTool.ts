import { Tool } from '@sparrowstack/tool';
import { getDirectoryStructureToolParams } from '@sparrowstack/tools/src/getDirectoryStucture/tool/getDirectoryStructureToolParams';

export const getDirectoryStructureTool = new Tool(
	getDirectoryStructureToolParams,
);

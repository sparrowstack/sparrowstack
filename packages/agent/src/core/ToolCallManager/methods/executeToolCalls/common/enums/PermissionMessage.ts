export enum PermissionMessage {
	AllowToolCallingAfterDenial = 'After this message enable tool calling for this tool and allow the user to call this tool again.',
	BlockToolCallingAfterDenial = 'After this message block tool calling for this tool and do not allow the user to call this tool again.',
}
